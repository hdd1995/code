caniuse.com  此网址可以查看标签或者属于支持浏览器情况
ie浏览器的兼容
ie6.7.8.9+
body{
background:red/9;     属性值的后面添加/9代表ie9以下的版本
}
属性的前面* ie78
属性的前面_ ie6


ie6常见的bug

1.双倍边距（float与margin产生）
.f1{float:left;dispaly:inline;}行级标记就不会出现次bug
2.三像素的bug;一个float的元素后面跟着一个非float的元素；
给添加float的元素添加一个margin-right:-3px;

css3属性兼容性处理：
css3某些属性添加一些私有前缀，判断属性还是属性值是否是css3新增

私有前缀
animation：  动画
-webkit-animation:  谷歌
-moz-animation:  火狐
-ms   微软
-o-   欧朋
background-image:




js三部分构成： ECMAscripte ecma欧洲计算机联盟   js核心（标准）es5   es6
   DOM  文档对象模型  documnent object model
   bom   浏览器对象   browser object model
   
 变量：为了存储数据（向内存申请空间）
 关键字  var
 js基本数据类型：number（数字），字符串string，boolean，null,undefine 
   对象数据类型：
   运算符（表达式）：+,-,*,/,%(求余数)   a%2=0;偶数
+除了做数学运算之外，如果+号两边有一个为字符串类型，那代表的就是字符串的拼接     '688'=6+'88'
-号：如果是数字才会进行数学运算，如果-号又一边为字符串（内容为数字组合），把字符串转为numbur     -82=6-'88'
运算失败就会返回NAN    NAN=6-'88JSAF'
